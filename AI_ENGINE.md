# Brain Bash AI engine

The mobile AI path uses `onnx-community/all-MiniLM-L6-v2-ONNX` through Transformers.js for semantic question diversity. The model selects less-similar questions from the validated local bank; it does not generate or alter factual answers.
